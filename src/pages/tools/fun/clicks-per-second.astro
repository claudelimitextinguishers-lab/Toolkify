---
import Layout from "../../../layouts/Layout.astro";

// Tool metadata
const name = "Clicks Per Second (CPS) Test";
const category = "fun";
const tier = "Fun Tools";
const description =
  "Test your mouse clicking or touchscreen tapping speed. Measure your clicks per second (CPS) over standard intervals and reach elite speed tiers.";

// SEO metadata
const seoTitle =
  "CPS Test – Free Clicks Per Second Test & Click Speed Counter";
const seoDescription =
  "Take a free CPS test to measure how many clicks per second you can do. This clicks per second tester counts your click speed over 1, 5, 10, and 60 seconds with live CPS counter, right-click support, and unblocked play.";
const seoKeywords =
  "cps test, clicks per second test, how many clicks per second test, clicks per second, how many clicks per second, clicks per second counter, clicks per second tester, cps test - clicks per second, fastest clicks per second, clicks per second game, mouse clicks per second, test clicks per second, clicks per second calculator, cps, cps tester, cps counter, cps test unblocked, cps test right click, cps test 1 second, cps test clicker";
---

<Layout
  title={seoTitle}
  description={seoDescription}
  keywords={seoKeywords}
  toolName={name}
  toolCategory={category}
  ogType="website"
  hideBanner={true}
>
  <!-- SEO JSON-LD: WebApplication -->
  <script type="application/ld+json" is:inline>
    {
      "@context": "https://schema.org",
      "@type": "WebApplication",
      "name": "Clicks Per Second (CPS) Test",
      "alternateName": "CPS Test – Clicks Per Second Click Speed Counter",
      "url": "https://toolkify.app/tools/fun/clicks-per-second",
      "description": "Free online CPS test that measures how many clicks per second you can do. This clicks per second tester counts mouse clicks and taps across 1s, 5s, 10s, and 60s windows with a live CPS counter, right-click support, audio-visual feedback, and elite speed tiers.",
      "applicationCategory": "GameApplication",
      "operatingSystem": "All",
      "browserRequirements": "Requires JavaScript. Requires HTML5.",
      "isAccessibleForFree": true,
      "keywords": "cps test, clicks per second test, clicks per second counter, cps counter, cps tester, mouse clicks per second, cps test unblocked, fastest clicks per second",
      "offers": {
        "@type": "Offer",
        "price": "0.00",
        "priceCurrency": "USD"
      }
    }
  </script>

  <!-- SEO JSON-LD: FAQPage -->
  <script type="application/ld+json" is:inline>
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "What is CPS?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>CPS stands for clicks per second. It is a metric used to measure your mouse clicking speed or touchscreen tapping rate within a specific timeframe.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "What does CPS stand for?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>CPS stands for Clicks Per Second. It is a speed metric representing the total number of click inputs registered within a one-second window.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "How many clicks per second?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>An average user can register 4 to 7 clicks per second. Experienced gamers and players utilizing specialized clicking methods can achieve 10 to 15 clicks per second or more.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "What is the world record for clicks per second?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>The official world record for click speed over a standard 5-second test window is 16 CPS (80 total clicks). However, utilizing drag-clicking or double-clicking mouse switches can result in short bursts exceeding 30 to 50 CPS.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "What is the average clicks per second?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>The average clicks per second for standard computer users is between 4 and 7 CPS.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "How many clicks per second is fast?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>Reaching 8 to 10 clicks per second is considered fast and competitive. Exceeding 12 CPS is exceptionally fast, placing you in an elite clicking bracket.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "How many clicks per second test?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>A clicks per second test measures click speed by counting the total number of registered mouse clicks or screen taps over a selected time period and dividing it by that duration.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "Is 10 clicks per second good?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>Yes, 10 clicks per second is very good. It is well above average and puts you in a highly competitive category for gaming and speed tapping.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "Is 11 clicks per second fast?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>Yes, 11 clicks per second is extremely fast. Achieving this speed consistently indicates excellent reflex control and advanced finger dexterity.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "What is the highest clicks per second?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>For standard single-finger clicking, the highest rate is roughly 15 to 22 CPS. Using advanced hardware-assisted or friction-based drag clicking, speeds of 30 to 100+ CPS can be temporarily registered.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "How to calculate clicks per second?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>To calculate clicks per second, divide your total click count by the duration of the test in seconds: CPS = Total Clicks / Seconds.</p>"
          }
        },
        {
          "@type": "Question",
          "name": "Who has the fastest clicks per second record?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "<p>Dylan Allred holds the official record on popular click speed test platforms, achieving an outstanding score of 16 CPS (80 clicks in 5 seconds).</p>"
          }
        }
      ]
    }
  </script>

  <section
    class="max-w-[1000px] w-full mx-auto px-4 py-3 sm:py-6 flex flex-col gap-3 sm:gap-4 animate-fade-in-up pb-16"
  >
    <!-- Breadcrumb & Header -->
    <div class="flex flex-col gap-1">
      <nav
        class="flex items-center gap-1.5 text-[10px] font-semibold text-slate uppercase"
        aria-label="Breadcrumb"
      >
        <a href="/" class="hover:text-primary transition-colors">Home</a>
        <span>/</span>
        <a href="/" class="hover:text-primary transition-colors">Tools</a>
        <span>/</span>
        <span class="text-brand-blue capitalize">{category}</span>
      </nav>
      <div
        class="flex flex-col sm:flex-row sm:items-center justify-between mt-0.5 gap-3"
      >
        <div class="flex items-center gap-2 flex-wrap">
          <h1
            class="font-display font-medium text-xl sm:text-2xl tracking-tight text-primary"
          >
            {name}
          </h1>
          <span
            class="bg-rose-light text-coral-dark text-[10px] py-0.5 px-2 rounded-full font-semibold uppercase whitespace-nowrap"
            >{tier}</span
          >
        </div>
        <div class="flex items-center gap-2 self-end sm:self-auto flex-wrap">
          <!-- Audio Toggle -->
          <button
            type="button"
            id="audio-toggle"
            class="inline-flex items-center justify-center w-8 h-8 bg-canvas hover:bg-surface border border-hairline text-slate hover:text-primary rounded-full transition-all cursor-pointer shadow-subtle"
            aria-label="Toggle sound effects"
            title="Toggle sound effects"
          >
            <svg
              id="sound-on-icon"
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M15.536 8.464a5 5 0 010 7.072m2.828-9.9a9 9 0 010 12.728M5.586 15H4a1 1 0 01-1-1v-4a1 1 0 011-1h1.586l4.707-4.707C10.923 3.663 12 4.109 12 5v14c0 .891-1.077 1.337-1.707.707L5.586 15z"
              ></path>
            </svg>
            <svg
              id="sound-off-icon"
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 hidden"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M5.586 15H4a1 1 0 01-1-1v-4a1 1 0 011-1h1.586l4.707-4.707C10.923 3.663 12 4.109 12 5v14c0 .891-1.077 1.337-1.707.707L5.586 15zm10.914-4.5l3 3m0-3l-3 3"
              ></path>
            </svg>
          </button>

          <!-- Share Tool Link Button -->
          <button
            type="button"
            id="share-tool-btn"
            class="inline-flex items-center gap-1.5 text-[11px] font-semibold px-2.5 py-1.5 bg-canvas hover:bg-surface border border-hairline text-slate hover:text-primary rounded-full transition-all cursor-pointer shadow-subtle"
            aria-label="Share tool link"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M8.684 10.742l5.028-2.513m0 7.544l-5.028-2.513M18 8a3 3 0 11-6 0 3 3 0 016 0zm-6 8a3 3 0 11-6 0 3 3 0 016 0zm6 0a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
            <span>Share</span>
          </button>

          <!-- How It Works Button -->
          <button
            type="button"
            id="how-it-works-btn"
            class="inline-flex items-center gap-1.5 text-[11px] font-semibold px-2.5 py-1.5 bg-canvas hover:bg-surface border border-hairline text-slate hover:text-primary rounded-full transition-all cursor-pointer shadow-subtle"
            aria-label="How clicks per second test works"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <span>How it works</span>
          </button>

          <!-- Reset Button -->
          <button
            type="button"
            id="reset-btn"
            class="inline-flex items-center gap-1.5 text-[11px] font-semibold px-3 py-1.5 bg-canvas hover:bg-surface border border-hairline text-slate hover:text-brand-coral hover:border-brand-coral rounded-full transition-all cursor-pointer shadow-subtle"
            aria-label="Reset test data"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-3.5 h-3.5"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M4 4v5h.582m15.356 2A8.001 8.001 0 1121.21 7.89M9 11l3-3 3 3m-3-3v12"
              ></path>
            </svg>
            <span>Reset</span>
          </button>
        </div>
      </div>
    </div>

    <!-- Workspace Container (Zero-Scroll Compact Layout) -->
    <div
      class="card-base p-4 sm:p-5 flex flex-col gap-4 dark-plum-theme relative overflow-hidden"
    >
      <!-- Background glow overlay for plum flavor -->
      <div
        class="absolute inset-0 bg-gradient-to-br from-plum-bg/40 to-plum-bg/10 pointer-events-none"
      >
      </div>

      <!-- Duration Selector / Options row -->
      <div
        class="flex items-center justify-between z-10 border-b border-plum-border/40 pb-3 flex-wrap gap-2"
      >
        <div class="flex items-center gap-2">
          <span
            class="text-[11px] uppercase font-bold text-plum-accent tracking-wider"
            >Duration:</span
          >
          <div class="flex items-center gap-1" id="duration-select-container">
            <button class="duration-btn active" data-seconds="5">5s</button>
            <button class="duration-btn" data-seconds="1">1s</button>
            <button class="duration-btn" data-seconds="10">10s</button>
            <button class="duration-btn" data-seconds="60">60s</button>
          </div>
        </div>
        <div
          class="text-[10px] text-plum-text-muted font-mono select-none"
          id="keyboard-hint"
        >
          Click or Tap the arena to start
        </div>
      </div>

      <div
        class="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-5 items-stretch z-10"
      >
        <!-- Left Pane: Interactive Click Arena -->
        <div class="flex flex-col gap-2 relative">
          <div class="flex items-center justify-between">
            <span class="text-[11px] font-semibold text-plum-text-bright"
              >Click Arena</span
            >
            <span
              class="text-[10px] font-mono text-plum-text-muted"
              id="cps-tracker">0.00 CPS</span
            >
          </div>

          <!-- Click Arena Box -->
          <div
            id="click-arena"
            tabindex="0"
            class="flex-1 min-h-[160px] sm:min-h-[320px] rounded-xl border border-plum-border bg-plum-surface flex flex-col items-center justify-center text-center p-4 sm:p-6 cursor-pointer select-none relative overflow-hidden transition-all duration-300 focus:outline-none touch-action-none"
            aria-label="Click speed test arena"
          >
            <!-- Particle Grid (Decorative background) -->
            <div
              class="absolute inset-0 grid grid-cols-8 grid-rows-8 opacity-[0.04] pointer-events-none transition-opacity duration-500"
              id="arena-grid"
            >
              {
                Array.from({ length: 64 }).map(() => (
                  <div class="border-[0.5px] border-plum-accent" />
                ))
              }
            </div>

            <!-- Linear Depleting Progress Indicator -->
            <div
              class="absolute top-0 left-0 h-1.5 bg-gradient-to-r from-plum-accent to-brand-coral transition-all duration-75 w-full pointer-events-none"
              id="timer-bar"
            >
            </div>

            <!-- Canvas for particle explosions -->
            <canvas
              id="particles-canvas"
              class="absolute inset-0 pointer-events-none z-0"></canvas>

            <!-- Main Content Elements -->
            <div
              class="z-10 flex flex-col items-center gap-1.5 sm:gap-3 w-full pointer-events-none"
            >
              <span
                id="arena-icon"
                class="text-3xl sm:text-5xl transition-transform duration-300 animate-float-slow"
                >🖱️</span
              >
              <h2
                id="arena-title"
                class="font-display font-bold text-base sm:text-2xl text-plum-text-bright leading-tight max-w-xs transition-colors duration-200"
              >
                Click to Start Test
              </h2>
              <p
                id="arena-instructions"
                class="text-[10px] sm:text-xs text-plum-text-muted max-w-xs leading-relaxed transition-colors duration-200"
              >
                The timer will start automatically on your very first click!
              </p>
            </div>
          </div>

          <!-- Dedicated Restart Button -->
          <button
            type="button"
            id="restart-test-btn"
            class="hidden w-full inline-flex items-center justify-center gap-1.5 text-xs font-semibold px-4 py-2 bg-plum-accent text-canvas rounded-full transition-all cursor-pointer shadow-subtle hover:bg-plum-accent/90 animate-fade-in-up mt-1"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M4 4v5h.582m15.356 2A8.001 8.001 0 1121.21 7.89M9 11l3-3 3 3m-3-3v12"
              ></path>
            </svg>
            <span>Restart Test</span>
          </button>
        </div>

        <!-- Right Pane: Performance Dashboard & Tiers -->
        <div
          class="flex flex-col justify-between p-3 sm:p-5 relative overflow-hidden rounded-xl border border-plum-border bg-plum-surface shadow-inner min-h-[180px] sm:min-h-[320px]"
        >
          <!-- Interactive background elements -->
          <div
            class="absolute top-0 right-0 w-32 h-32 bg-plum-accent/10 rounded-full blur-2xl pointer-events-none"
          >
          </div>

          <div
            class="flex flex-col gap-2 sm:gap-4 z-10 w-full h-full justify-between"
          >
            <!-- Results Header -->
            <div
              class="flex items-center justify-between border-b border-plum-border/40 pb-1.5 sm:pb-2"
            >
              <span
                class="text-[10px] uppercase font-extrabold text-plum-accent tracking-widest"
                >CPS Dashboard</span
              >
              <div class="flex items-center gap-1.5">
                <span
                  class="text-[10px] font-mono text-plum-text-muted"
                  id="state-indicator">Status: Idle</span
                >
                <span class="flex h-2 w-2 relative">
                  <span
                    id="status-pulse"
                    class="animate-ping absolute inline-flex h-full w-full rounded-full bg-stone opacity-75"
                  ></span>
                  <span
                    id="status-dot"
                    class="relative inline-flex rounded-full h-2 w-2 bg-stone"
                  ></span>
                </span>
              </div>
            </div>

            <!-- Themed Visual Container / Results Reveal -->
            <div
              id="themed-reveal-container"
              class="flex flex-col items-center justify-center py-2 sm:py-4 relative min-h-[90px] sm:min-h-[160px] bg-black/20 rounded-xl border border-plum-border/20 backdrop-blur-sm shadow-inner transition-all duration-300"
            >
              <!-- Default state / live dashboard readout -->
              <div
                id="dashboard-readout"
                class="flex flex-col items-center gap-0.5 sm:gap-1 w-full px-4"
              >
                <span
                  class="text-[9px] sm:text-[10px] uppercase font-bold text-plum-text-muted"
                  >Clicks Per Second</span
                >
                <div class="flex items-baseline gap-1 mt-0.5">
                  <span
                    id="output-cps"
                    class="font-display font-black text-4xl sm:text-6xl text-plum-text-bright font-mono tracking-tight leading-none"
                    >0.00</span
                  >
                  <span
                    class="text-[10px] sm:text-xs font-semibold text-plum-accent"
                    >CPS</span
                  >
                </div>

                <!-- Quick secondary metrics -->
                <div
                  class="grid grid-cols-2 gap-4 w-full mt-2 sm:mt-3 pt-2 sm:pt-3 border-t border-plum-border/30 text-center"
                >
                  <div class="flex flex-col">
                    <span
                      class="text-[8px] uppercase font-bold text-plum-text-muted"
                      >Total Clicks</span
                    >
                    <span
                      id="output-total-clicks"
                      class="text-xs sm:text-sm font-mono font-bold text-plum-text-bright"
                      >0</span
                    >
                  </div>
                  <div class="flex flex-col">
                    <span
                      class="text-[8px] uppercase font-bold text-plum-text-muted"
                      >Remaining Time</span
                    >
                    <span
                      id="output-time-left"
                      class="text-xs sm:text-sm font-mono font-bold text-plum-text-bright"
                      >--</span
                    >
                  </div>
                </div>
              </div>

              <!-- Animated Cockpit / Speedlines / Neon Targets for Tier Payoffs -->
              <div
                id="pilot-radar"
                class="radar-hud-effect absolute inset-0 rounded-xl overflow-hidden pointer-events-none hidden"
              >
                <div class="radar-line"></div>
                <div class="radar-crosshair"></div>
                <div
                  class="hud-text absolute bottom-2 left-2 text-[8px] font-mono text-brand-yellow font-bold"
                >
                  MACH_SPEED: PILOT_LOCKED
                </div>
              </div>

              <div
                id="racer-speedlines"
                class="speedline-effect absolute inset-0 rounded-xl overflow-hidden pointer-events-none hidden"
              >
                <div class="checkered-flag-bg"></div>
                <div class="line"></div>
                <div class="line"></div>
                <div class="line"></div>
                <div class="line"></div>
              </div>

              <div
                id="gamer-neon"
                class="neon-effect absolute inset-0 rounded-xl overflow-hidden pointer-events-none hidden"
              >
                <div class="neon-ring"></div>
                <div class="gaming-crosshair"></div>
              </div>

              <div
                id="sloth-anim"
                class="sloth-effect absolute inset-0 rounded-xl overflow-hidden pointer-events-none hidden flex items-center justify-center"
              >
                <span class="text-4xl animate-bounce">💤</span>
              </div>
            </div>

            <!-- Dynamic Advice / Classification -->
            <div
              id="classification-box"
              class="text-xs text-plum-text-muted leading-relaxed pt-2 sm:pt-3 border-t border-plum-border/40"
            >
              <div class="flex flex-wrap gap-1.5 items-center">
                <span
                  class="text-[9px] uppercase font-extrabold text-plum-text-muted"
                  >Tier Classification:</span
                >
                <span
                  id="classification-badge"
                  class="bg-plum-border/30 text-plum-text-muted text-[9px] py-0.5 px-2 rounded-full font-bold uppercase tracking-wider"
                  >Not Classified</span
                >
              </div>
              <p
                id="main-context-text"
                class="font-medium text-[10px] sm:text-[11px] mt-1 sm:mt-1.5 text-plum-text-muted"
              >
                Complete a test to discover your clicking tier. Select a
                duration and start tapping.
              </p>
            </div>

            <!-- Action buttons / Share -->
            <div
              id="action-btn-row"
              class="hidden items-center gap-2 pt-1"
            >
              <button
                type="button"
                id="share-btn"
                class="flex-1 inline-flex items-center justify-center gap-1.5 text-xs font-semibold px-4 py-2 bg-plum-accent text-canvas rounded-full transition-all cursor-pointer shadow-subtle hover:bg-plum-accent/90"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  class="w-4 h-4"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M8.684 10.742l4.622-2.311m0 0a3 3 0 10-3.664-3.664L8.684 10.742m0 0a3 3 0 100 4.516l4.622 2.311m0 0a3 3 0 103.664-3.664l-4.622-2.311"
                  ></path>
                </svg>
                <span>Share Results</span>
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Educational details panel -->
    <div class="card-base flex flex-col gap-4 p-4 sm:p-5 mt-2">
      <h3
        class="font-display font-semibold text-sm sm:text-base text-primary flex items-center gap-1.5"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="w-4 h-4 text-brand-blue"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          stroke-width="2.5"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
          ></path>
        </svg>
        Understanding Clicks Per Second (CPS)
      </h3>
      <div
        class="text-[11px] sm:text-xs text-slate leading-relaxed flex flex-col gap-2"
      >
        <p>
          Clicks Per Second (CPS) is a standard benchmark used primarily in
          competitive gaming (especially in titles like Minecraft, OSU!, and
          MOBAs) to measure a player's motor speed and physical reflex
          capability.
        </p>
        <p class="font-semibold text-charcoal">Advanced Clicking Techniques:</p>
        <ul class="list-disc pl-4 flex flex-col gap-1.5 text-slate font-medium">
          <li>
            <strong>Jitter Clicking:</strong> Tensing the forearm and wrist muscles
            to vibrate the finger rapidly on the mouse button, pushing CPS up to 10–14.
          </li>
          <li>
            <strong>Butterfly Clicking:</strong> Alternating clicks between the index
            and middle fingers on the same button to achieve 15–20 CPS.
          </li>
          <li>
            <strong>Drag Clicking:</strong> Dragging a textured finger down the mouse
            button to utilize friction, generating rapid mechanical switch triggers
            (can reach 30+ CPS).
          </li>
        </ul>
        <p class="text-[10px] text-stone mt-1">
          Hardware latency from USB polling rates, mechanical debounce
          algorithms, and browser input scheduling can affect registered speeds.
        </p>
      </div>
    </div>

    <!-- Security & Privacy Guard Footer Tag -->
    <div class="flex items-center justify-between text-[10px] text-slate px-1">
      <span class="flex items-center gap-1">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="w-3 h-3 text-success-accent"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          stroke-width="2.5"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"
          ></path>
        </svg>
        100% Client-Side Speed Evaluation
      </span>
      <span>v1.0.0</span>
    </div>
  </section>

  <!-- SEO Content Article -->
  <section
    class="max-w-[760px] w-full mx-auto px-4 pb-20 flex flex-col gap-8 text-slate"
    aria-labelledby="about-tool-heading"
  >
    <article class="flex flex-col gap-4">
      <h2
        id="about-tool-heading"
        class="font-display font-semibold text-xl sm:text-2xl text-primary tracking-tight"
      >
        Free CPS Test – Measure Your Clicks Per Second Online
      </h2>
      <p class="text-sm leading-relaxed">
        This <strong>CPS test</strong> is a free, browser-based
        <strong>clicks per second test</strong> that measures exactly how fast
        you can click your mouse or tap your screen. Click the arena to start the
        timer, then click as quickly as possible until time runs out. The
        built-in <strong>clicks per second counter</strong> divides your total
        clicks by the duration to calculate your average <strong>CPS</strong>,
        updating live as you go. Because this <strong>clicks per second tester</strong>
        runs entirely in your browser, it needs no download, no login, and never
        uploads your results — making it a reliable
        <strong>CPS test unblocked</strong> tool on school and office networks.
      </p>

      <h3 class="font-display font-semibold text-lg text-primary mt-2">
        What does clicks per second (CPS) mean?
      </h3>
      <p class="text-sm leading-relaxed">
        Clicks per second, or CPS, is the number of mouse clicks you can register
        in one second. When people ask <strong>how many clicks per second</strong>
        they can do, they are describing their click speed. To find out, you run a
        <strong>test clicks per second</strong> over a fixed window and let the
        tool average the result. Our <strong>CPS counter</strong> works like a
        <strong>clicks per second calculator</strong>: it tracks every click,
        keeps a running total, and converts it into a precise CPS figure using
        high-resolution browser timers, so the score reflects your true
        <strong>mouse clicks per second</strong> rather than a rough estimate.
      </p>

      <h3 class="font-display font-semibold text-lg text-primary mt-2">
        Choose your test duration: 1, 5, 10, or 60 seconds
      </h3>
      <p class="text-sm leading-relaxed">
        Different durations test different skills. A <strong>CPS test 1 second</strong>
        measures pure burst speed — how explosively you can click in a single
        tick. The 5-second and 10-second windows are the most popular benchmarks
        and give the most stable, comparable scores for a typical
        <strong>clicks per second game</strong> session. The 60-second mode is an
        endurance challenge that shows how well you maintain speed before your hand
        tires. Switch durations any time before you begin and use this
        <strong>cps test - clicks per second</strong> tool to track each one
        separately.
      </p>

      <h3 class="font-display font-semibold text-lg text-primary mt-2">
        How many clicks per second is good?
      </h3>
      <p class="text-sm leading-relaxed">
        On a standard <strong>how many clicks per second test</strong>, the average
        desktop user lands between <strong>4 and 7 CPS</strong> with normal
        clicking. Anything from 7 to 10 CPS is a strong, competitive score, and
        7+ puts you in pro-gamer territory. The <strong>fastest clicks per second</strong>
        results — over 12 to 14 CPS — usually come from specialized techniques.
        After each round, this <strong>CPS tester</strong> classifies your result
        into a fun tier, from Sleepy Sloth and Casual Clicker up to Pro Gamer, F1
        Racer, and Fighter Jet Pilot, so you instantly see where you rank.
      </p>

      <h3 class="font-display font-semibold text-lg text-primary mt-2">
        Clicking techniques to boost your CPS
      </h3>
      <p class="text-sm leading-relaxed">
        If you want the <strong>fastest clicks per second</strong> score, three
        techniques dominate competitive play. <strong>Jitter clicking</strong>
        tenses the forearm to vibrate the finger and can push CPS to 10–14.
        <strong>Butterfly clicking</strong> alternates two fingers on the same
        button to reach 15–20 CPS. <strong>Drag clicking</strong> drags a textured
        finger across the button to fire many switch triggers at once, sometimes
        exceeding 30 CPS. Hardware matters too: a light gaming mouse, low USB
        polling latency, and a relaxed grip all help you
        <strong>test clicks per second</strong> at your true potential.
      </p>

      <h3 class="font-display font-semibold text-lg text-primary mt-2">
        Right click, spacebar, and touchscreen support
      </h3>
      <p class="text-sm leading-relaxed">
        This tool is more than a left-click counter. Because the arena listens for
        any pointer press, you can run a <strong>CPS test right click</strong>,
        test your mouse wheel click, or tap on a phone or tablet — perfect for
        comparing buttons. With the arena focused you can also use the spacebar.
        Whether you treat it as a serious
        <strong>clicks per second tester</strong> or a casual
        <strong>cps test clicker</strong> challenge, every input is measured the
        same precise way.
      </p>

      <h3 class="font-display font-semibold text-lg text-primary mt-2">
        Why use this clicks per second test?
      </h3>
      <p class="text-sm leading-relaxed">
        This <strong>CPS test</strong> was built for accuracy and speed. It uses
        high-resolution timers for exact measurement, gives satisfying audio and
        particle feedback on every click, and lets you share your score in one tap.
        Whether you are a gamer benchmarking your <strong>mouse clicks per second</strong>,
        a Minecraft player chasing more taps, or just curious
        <strong>how many clicks per second</strong> you can manage, this free
        <strong>clicks per second counter</strong> gives instant, private, and
        shareable results on any device.
      </p>
    </article>

    <!-- FAQ Accordion Section -->
    <div class="flex flex-col gap-6 max-w-4xl mx-auto w-full mt-12">
      <h2 class="font-display font-semibold text-2xl sm:text-3xl tracking-tight text-primary text-center mb-2">
        Clicks Per Second (CPS) FAQs
      </h2>
      <p class="text-xs text-slate text-center mb-6">
        Find answers to the most common questions about clicking speed, measuring CPS, and training to click faster.
      </p>
      
      <div class="flex flex-col border border-hairline rounded-xl bg-canvas overflow-hidden shadow-subtle divide-y divide-hairline">
        <!-- FAQ 1 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>What is CPS?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>CPS stands for clicks per second. It is a metric used to measure your mouse clicking speed or touchscreen tapping rate within a specific timeframe.</p>
          </div>
        </div>

        <!-- FAQ 2 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>What does CPS stand for?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>CPS stands for Clicks Per Second. It is a speed metric representing the total number of click inputs registered within a one-second window.</p>
          </div>
        </div>

        <!-- FAQ 3 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>How many clicks per second?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>An average user can register 4 to 7 clicks per second. Experienced gamers and players utilizing specialized clicking methods can achieve 10 to 15 clicks per second or more.</p>
          </div>
        </div>

        <!-- FAQ 4 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>What is the world record for clicks per second?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>The official world record for click speed over a standard 5-second test window is 16 CPS (80 total clicks). However, utilizing drag-clicking or double-clicking mouse switches can result in short bursts exceeding 30 to 50 CPS.</p>
          </div>
        </div>

        <!-- FAQ 5 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>What is the average clicks per second?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>The average clicks per second for standard computer users is between 4 and 7 CPS.</p>
          </div>
        </div>

        <!-- FAQ 6 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>How many clicks per second is fast?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>Reaching 8 to 10 clicks per second is considered fast and competitive. Exceeding 12 CPS is exceptionally fast, placing you in an elite clicking bracket.</p>
          </div>
        </div>

        <!-- FAQ 7 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>How many clicks per second test?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>A clicks per second test measures click speed by counting the total number of registered mouse clicks or screen taps over a selected time period and dividing it by that duration.</p>
          </div>
        </div>

        <!-- FAQ 8 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>Is 10 clicks per second good?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>Yes, 10 clicks per second is very good. It is well above average and puts you in a highly competitive category for gaming and speed tapping.</p>
          </div>
        </div>

        <!-- FAQ 9 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>Is 11 clicks per second fast?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>Yes, 11 clicks per second is extremely fast. Achieving this speed consistently indicates excellent reflex control and advanced finger dexterity.</p>
          </div>
        </div>

        <!-- FAQ 10 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>What is the highest clicks per second?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>For standard single-finger clicking, the highest rate is roughly 15 to 22 CPS. Using advanced hardware-assisted or friction-based drag clicking, speeds of 30 to 100+ CPS can be temporarily registered.</p>
          </div>
        </div>

        <!-- FAQ 11 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>How to calculate clicks per second?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>To calculate clicks per second, divide your total click count by the duration of the test in seconds: CPS = Total Clicks / Seconds.</p>
          </div>
        </div>

        <!-- FAQ 12 -->
        <div class="faq-accordion-item group">
          <button
            type="button"
            class="faq-toggle-btn w-full text-left font-display font-medium text-[15px] sm:text-base text-primary flex items-center justify-between cursor-pointer px-6 py-4.5 hover:bg-surface-soft transition-colors duration-200"
            aria-expanded="false"
          >
            <span>Who has the fastest clicks per second record?</span>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-4 h-4 text-stone group-hover:text-primary transition-transform duration-200 shrink-0 ml-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2.5"
            >
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-6 pb-5 pt-1 text-sm text-slate leading-relaxed border-t border-hairline-soft/30 bg-surface-soft/10">
            <p>Dylan Allred holds the official record on popular click speed test platforms, achieving an outstanding score of 16 CPS (80 clicks in 5 seconds).</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Client-side Logic Module -->
  <script>
    type TestState = "idle" | "playing" | "finished";
    let currentState: TestState = "idle";

    // State parameters
    let totalClicks = 0;
    let selectedDuration = 5; // Default 5s
    let startTime = 0;
    let timerFrameId: number | null = null;
    let soundEnabled = true;
    let audioCtx: AudioContext | null = null;

    // DOM Elements
    const clickArena = document.getElementById("click-arena");
    const arenaTitle = document.getElementById("arena-title");
    const arenaInstructions = document.getElementById("arena-instructions");
    const arenaIcon = document.getElementById("arena-icon");
    const timerBar = document.getElementById("timer-bar");
    const cpsTracker = document.getElementById("cps-tracker");
    const stateIndicator = document.getElementById("state-indicator");
    const statusDot = document.getElementById("status-dot");
    const statusPulse = document.getElementById("status-pulse");

    const outputCPS = document.getElementById("output-cps");
    const outputTotalClicks = document.getElementById("output-total-clicks");
    const outputTimeLeft = document.getElementById("output-time-left");

    const durationBtns = document.querySelectorAll(".duration-btn");
    const audioToggle = document.getElementById("audio-toggle");
    const soundOnIcon = document.getElementById("sound-on-icon");
    const soundOffIcon = document.getElementById("sound-off-icon");
    const resetBtn = document.getElementById("reset-btn");
    const shareBtn = document.getElementById("share-btn");
    const actionBtnRow = document.getElementById("action-btn-row");
    const restartTestBtn = document.getElementById("restart-test-btn");

    // Classification elements
    const classificationBadge = document.getElementById("classification-badge");
    const mainContextText = document.getElementById("main-context-text");

    // Tier Anim Panels
    const pilotRadar = document.getElementById("pilot-radar");
    const racerSpeedlines = document.getElementById("racer-speedlines");
    const gamerNeon = document.getElementById("gamer-neon");
    const slothAnim = document.getElementById("sloth-anim");
    const dashboardReadout = document.getElementById("dashboard-readout");

    // Particles setup
    const canvas = document.getElementById(
      "particles-canvas",
    ) as HTMLCanvasElement;
    const ctx = canvas ? canvas.getContext("2d") : null;
    let particles: Array<{
      x: number;
      y: number;
      vx: number;
      vy: number;
      radius: number;
      color: string;
      alpha: number;
    }> = [];
    let animating = false;

    function resizeCanvas() {
      if (canvas && clickArena) {
        canvas.width = clickArena.clientWidth;
        canvas.height = clickArena.clientHeight;
      }
    }
    resizeCanvas();
    window.addEventListener("resize", resizeCanvas);

    // Audio synthesizer helper
    function getAudioContext() {
      if (!audioCtx) {
        audioCtx = new (
          window.AudioContext || (window as any).webkitAudioContext
        )();
      }
      if (audioCtx.state === "suspended") {
        audioCtx.resume();
      }
      return audioCtx;
    }

    function playTone(
      freq: number,
      type: OscillatorType,
      duration: number,
      volume: number = 0.1,
    ) {
      if (!soundEnabled) return;
      try {
        const ctx = getAudioContext();
        const osc = ctx.createOscillator();
        const gainNode = ctx.createGain();

        osc.type = type;
        osc.frequency.setValueAtTime(freq, ctx.currentTime);

        gainNode.gain.setValueAtTime(volume, ctx.currentTime);
        gainNode.gain.exponentialRampToValueAtTime(
          0.0001,
          ctx.currentTime + duration,
        );

        osc.connect(gainNode);
        gainNode.connect(ctx.destination);

        osc.start();
        osc.stop(ctx.currentTime + duration);
      } catch (err) {
        console.warn("AudioContext playback error:", err);
      }
    }

    function spawnParticles(x: number, y: number) {
      if (!ctx) return;
      const count = 12;
      const colors = ["#e040a9", "#ff9999", "#ffffff", "#ffd02f"];
      for (let i = 0; i < count; i++) {
        const angle = Math.random() * Math.PI * 2;
        const speed = 1 + Math.random() * 4;
        particles.push({
          x,
          y,
          vx: Math.cos(angle) * speed,
          vy: Math.sin(angle) * speed,
          radius: 2 + Math.random() * 3,
          color: colors[Math.floor(Math.random() * colors.length)],
          alpha: 1,
        });
      }
      if (!animating) {
        animateParticles();
      }
    }

    function animateParticles() {
      if (!ctx || !canvas) return;
      if (particles.length === 0 && currentState !== "playing") {
        animating = false;
        return;
      }
      animating = true;
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      particles = particles.filter(p => {
        p.x += p.vx;
        p.y += p.vy;
        p.alpha -= 0.04;
        if (p.alpha <= 0) return false;
        ctx!.save();
        ctx!.globalAlpha = p.alpha;
        ctx!.beginPath();
        ctx!.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
        ctx!.fillStyle = p.color;
        ctx!.fill();
        ctx!.restore();
        return true;
      });
      requestAnimationFrame(animateParticles);
    }
    animateParticles();

    // Reset and Initial values
    function init() {
      resetTest();
    }

    function resetTest() {
      currentState = "idle";
      totalClicks = 0;
      if (timerFrameId) {
        cancelAnimationFrame(timerFrameId);
        timerFrameId = null;
      }

      // Reset UI elements
      if (timerBar) timerBar.style.width = "100%";
      if (cpsTracker) cpsTracker.textContent = "0.00 CPS";
      if (stateIndicator) stateIndicator.textContent = "Status: Idle";
      if (arenaIcon) arenaIcon.textContent = "🖱️";
      if (arenaTitle) arenaTitle.textContent = "Click to Start Test";
      if (arenaInstructions)
        arenaInstructions.textContent =
          "The timer will start automatically on your very first click!";

      if (statusDot)
        statusDot.className =
          "relative inline-flex rounded-full h-2 w-2 bg-stone";
      if (statusPulse)
        statusPulse.className =
          "animate-ping absolute inline-flex h-full w-full rounded-full bg-stone opacity-75";

      if (outputCPS) outputCPS.textContent = "0.00";
      if (outputTotalClicks) outputTotalClicks.textContent = "0";
      if (outputTimeLeft)
        outputTimeLeft.textContent = selectedDuration.toFixed(1) + "s";

      if (classificationBadge) {
        classificationBadge.textContent = "Not Classified";
        classificationBadge.className =
          "bg-plum-border/30 text-plum-text-muted text-[9px] py-0.5 px-2 rounded-full font-bold uppercase tracking-wider";
      }
      if (mainContextText) {
        mainContextText.textContent =
          "Complete a test to discover your clicking tier. Select a duration and start tapping.";
      }

      // Hide all overlay anim components
      pilotRadar?.classList.add("hidden");
      racerSpeedlines?.classList.add("hidden");
      gamerNeon?.classList.add("hidden");
      slothAnim?.classList.add("hidden");
      dashboardReadout?.classList.remove("hidden");
      actionBtnRow?.classList.add("hidden");
      actionBtnRow?.classList.remove("flex");
      restartTestBtn?.classList.add("hidden");
    }

    // Timer Loop
    function updateTimer() {
      if (currentState !== "playing") return;
      const now = performance.now();
      const elapsed = (now - startTime) / 1000;
      const remaining = Math.max(0, selectedDuration - elapsed);

      // Update linear timer bar
      if (timerBar) {
        const percent = (remaining / selectedDuration) * 100;
        timerBar.style.width = `${percent}%`;
      }

      // Update stats dashboard
      const currentCPS = elapsed > 0 ? totalClicks / elapsed : 0;
      if (cpsTracker) cpsTracker.textContent = `${currentCPS.toFixed(2)} CPS`;
      if (outputCPS) outputCPS.textContent = currentCPS.toFixed(2);
      if (outputTimeLeft)
        outputTimeLeft.textContent = `${remaining.toFixed(1)}s`;

      if (remaining <= 0) {
        finishTest();
      } else {
        timerFrameId = requestAnimationFrame(updateTimer);
      }
    }

    function startTest() {
      currentState = "playing";
      startTime = performance.now();
      totalClicks = 1; // Count first click immediately
      if (outputTotalClicks) outputTotalClicks.textContent = "1";
      if (stateIndicator) stateIndicator.textContent = "Status: Testing";

      if (statusDot)
        statusDot.className =
          "relative inline-flex rounded-full h-2 w-2 bg-rose-500";
      if (statusPulse)
        statusPulse.className =
          "animate-ping absolute inline-flex h-full w-full rounded-full bg-rose-500 opacity-75";

      if (arenaTitle) arenaTitle.textContent = "CLICK FAST!";
      if (arenaInstructions)
        arenaInstructions.textContent = "Keep clicking as fast as you can!";
      if (arenaIcon) arenaIcon.textContent = "⚡";

      playTone(440, "sine", 0.1, 0.1);
      timerFrameId = requestAnimationFrame(updateTimer);
    }

    function registerClick(e: PointerEvent) {
      if (currentState === "finished") {
        return;
      }

      // Handle canvas coordinates
      if (clickArena) {
        const rect = clickArena.getBoundingClientRect();
        const clickX = e.clientX - rect.left;
        const clickY = e.clientY - rect.top;
        spawnParticles(clickX, clickY);
      }

      if (currentState === "idle") {
        startTest();
      } else if (currentState === "playing") {
        totalClicks++;
        if (outputTotalClicks)
          outputTotalClicks.textContent = totalClicks.toString();
        playTone(600 + Math.min(totalClicks * 5, 400), "sine", 0.08, 0.15);

        // Instant click scale feedback
        if (clickArena) {
          clickArena.style.transform = "scale(0.97)";
          setTimeout(() => {
            if (clickArena) clickArena.style.transform = "";
          }, 50);
        }
      }
    }

    function finishTest() {
      currentState = "finished";
      if (timerFrameId) cancelAnimationFrame(timerFrameId);
      if (timerBar) timerBar.style.width = "0%";
      if (stateIndicator) stateIndicator.textContent = "Status: Finished";

      if (statusDot)
        statusDot.className =
          "relative inline-flex rounded-full h-2 w-2 bg-emerald-500";
      if (statusPulse)
        statusPulse.className =
          "animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-500 opacity-75";

      if (arenaTitle) arenaTitle.textContent = "Time Up!";
      if (arenaInstructions)
        arenaInstructions.textContent = "Review your score in the dashboard.";
      if (arenaIcon) arenaIcon.textContent = "🏆";

      const finalCPS = totalClicks / selectedDuration;
      if (outputCPS) outputCPS.textContent = finalCPS.toFixed(2);
      if (outputTimeLeft) outputTimeLeft.textContent = "0.0s";

      playTone(523.25, "triangle", 0.15, 0.15); // C5
      setTimeout(() => playTone(659.25, "triangle", 0.15, 0.15), 100); // E5
      setTimeout(() => playTone(783.99, "triangle", 0.15, 0.15), 200); // G5
      setTimeout(() => playTone(1046.5, "triangle", 0.3, 0.2), 300); // C6

      // Evaluate Tiers
      evaluateCPS(finalCPS);

      actionBtnRow?.classList.remove("hidden");
      actionBtnRow?.classList.add("flex");
      restartTestBtn?.classList.remove("hidden");
    }

    function evaluateCPS(cps: number) {
      let classification = "";
      let badgeStyle = "";
      let adviceMsg = "";

      // Hide all overlay anim components first
      pilotRadar?.classList.add("hidden");
      racerSpeedlines?.classList.add("hidden");
      gamerNeon?.classList.add("hidden");
      slothAnim?.classList.add("hidden");
      dashboardReadout?.classList.remove("hidden");

      if (cps >= 12) {
        classification = "Fighter Jet Pilot";
        badgeStyle =
          "bg-brand-yellow text-primary border border-brand-yellow-deep shadow-[0_0_12px_rgba(252,185,0,0.4)]";
        adviceMsg = `Super-sonic speed! Your CPS of ${cps.toFixed(2)} is comparable to elite fighter jet pilots. Incredible finger speed.`;
        pilotRadar?.classList.remove("hidden");
        dashboardReadout?.classList.add("hidden");
      } else if (cps >= 10 && cps < 12) {
        classification = "F1 Racer";
        badgeStyle =
          "bg-brand-blue text-canvas border border-blue-pressed shadow-[0_0_12px_rgba(66,98,255,0.4)]";
        adviceMsg = `High-octane clicker! An average of ${cps.toFixed(2)} clicks per second places you in F1 racing driver tiers.`;
        racerSpeedlines?.classList.remove("hidden");
        dashboardReadout?.classList.add("hidden");
      } else if (cps >= 7 && cps < 10) {
        classification = "Pro Gamer";
        badgeStyle =
          "bg-success-accent text-canvas border border-moss-dark shadow-[0_0_12px_rgba(0,180,115,0.4)]";
        adviceMsg = `Twitch reflex locked! An average of ${cps.toFixed(2)} CPS is perfect for competitive esports.`;
        gamerNeon?.classList.remove("hidden");
        dashboardReadout?.classList.add("hidden");
      } else if (cps >= 4 && cps < 7) {
        classification = "Casual Clicker";
        badgeStyle =
          "bg-plum-border/40 border border-plum-accent text-plum-text-bright";
        adviceMsg = `Good effort! Your clicking speed of ${cps.toFixed(2)} CPS is within the standard healthy average for desktop users.`;
      } else {
        classification = "Sleepy Sloth";
        badgeStyle = "bg-brand-coral text-canvas border border-coral-dark";
        adviceMsg = `Reflex speed is ${cps.toFixed(2)} CPS. Wake up, close background tabs, and try again!`;
        slothAnim?.classList.remove("hidden");
        dashboardReadout?.classList.add("hidden");
      }

      if (classificationBadge) {
        classificationBadge.textContent = classification;
        classificationBadge.className = `text-[9px] py-0.5 px-2 rounded-full font-bold uppercase tracking-wider ${badgeStyle}`;
      }

      if (mainContextText) {
        mainContextText.textContent = adviceMsg;
      }
    }

    // Attach Event Listeners
    clickArena?.addEventListener("pointerdown", (e: PointerEvent) => {
      e.preventDefault();
      getAudioContext();
      registerClick(e);
    });

    // Spacebar trigger handler
    clickArena?.addEventListener("keydown", (e: KeyboardEvent) => {
      if (e.code === "Space") {
        e.preventDefault();
        getAudioContext();
        // Emulate pointer event coordinates for center
        if (clickArena) {
          const rect = clickArena.getBoundingClientRect();
          const pointerEvent = new PointerEvent("pointerdown", {
            clientX: rect.left + rect.width / 2,
            clientY: rect.top + rect.height / 2,
          });
          registerClick(pointerEvent);
        }
      }
    });

    // Reset button
    resetBtn?.addEventListener("click", () => {
      resetTest();
      if (window.history.pushState) {
        const newurl = window.location.protocol + "//" + window.location.host + window.location.pathname;
        window.history.pushState({path:newurl}, '', newurl);
      }
    });

    // Dedicated Restart button
    restartTestBtn?.addEventListener("click", () => {
      resetTest();
    });

    // Duration buttons
    durationBtns.forEach((btn) => {
      btn.addEventListener("click", (e) => {
        const target = e.currentTarget as HTMLButtonElement;
        if (currentState === "playing") {
          target.classList.add("opacity-50", "cursor-not-allowed");
          setTimeout(() => {
            target.classList.remove("opacity-50", "cursor-not-allowed");
          }, 400);
          return;
        }
        selectedDuration = parseInt(
          target.getAttribute("data-seconds") || "5",
          10,
        );
        if (currentState === "finished") {
          resetTest();
        }
        durationBtns.forEach((b) => b.classList.remove("active"));
        target.classList.add("active");
        if (outputTimeLeft)
          outputTimeLeft.textContent = selectedDuration.toFixed(1) + "s";
      });
    });

    // Audio Toggle Setting
    audioToggle?.addEventListener("click", () => {
      soundEnabled = !soundEnabled;
      if (soundEnabled) {
        soundOnIcon?.classList.remove("hidden");
        soundOffIcon?.classList.add("hidden");
      } else {
        soundOnIcon?.classList.add("hidden");
        soundOffIcon?.classList.remove("hidden");
      }
    });

    // Share results copy to clipboard
    shareBtn?.addEventListener("click", () => {
      const finalCPS = totalClicks / selectedDuration;
      const textToCopy = `I scored ${finalCPS.toFixed(2)} CPS (Clicks Per Second) on TOOLKIFY CPS Test! Duration: ${selectedDuration}s. Try to beat my score!`;
      navigator.clipboard
        .writeText(textToCopy)
        .then(() => {
          const span = shareBtn.querySelector("span");
          if (span) {
            const originalText = span.textContent || "Share Results";
            span.textContent = "Copied to Clipboard!";
            setTimeout(() => {
              span.textContent = originalText;
            }, 2000);
          }
        })
        .catch((err) => {
          console.error("Failed to copy text: ", err);
        });
    });

    // Share tool URL logic
    const shareToolBtn = document.getElementById("share-tool-btn");
    shareToolBtn?.addEventListener("click", () => {
      const url = new URL(window.location.origin + window.location.pathname);
      url.searchParams.set("duration", selectedDuration.toString());

      const shareData = {
        title: document.title || "Clicks Per Second Test",
        url: url.toString()
      };

      if (navigator.share) {
        navigator.share(shareData).catch((err) => {
          if (err.name !== "AbortError") {
            copyToClipboard(url.toString());
          }
        });
      } else {
        copyToClipboard(url.toString());
      }

      function copyToClipboard(text: string) {
        if (navigator.clipboard && navigator.clipboard.writeText) {
          navigator.clipboard.writeText(text).then(showSuccess).catch(() => fallbackCopy(text));
        } else {
          fallbackCopy(text);
        }
      }

      function fallbackCopy(text: string) {
        const textArea = document.createElement("textarea");
        textArea.value = text;
        textArea.style.position = "fixed";
        textArea.style.opacity = "0";
        document.body.appendChild(textArea);
        textArea.select();
        try {
          document.execCommand("copy");
          showSuccess();
        } catch (err) {
          console.error(err);
        }
        document.body.removeChild(textArea);
      }

      function showSuccess() {
        if (shareToolBtn) {
          const span = shareToolBtn.querySelector("span");
          if (span) {
            const originalText = span.textContent || "Share";
            span.textContent = "Link Copied!";
            shareToolBtn.classList.add("text-success-accent", "border-success-accent");
            setTimeout(() => {
              span.textContent = originalText;
              shareToolBtn.classList.remove("text-success-accent", "border-success-accent");
            }, 2000);
          }
        }
      }
    });

    // How it works modal events
    const howItWorksBtn = document.getElementById("how-it-works-btn");
    const howItWorksModal = document.getElementById("how-it-works-modal");
    const closeModalBtn = document.getElementById("close-modal-btn");
    const modalOkBtn = document.getElementById("modal-ok-btn");

    howItWorksBtn?.addEventListener("click", () => howItWorksModal?.classList.remove("hidden"));
    closeModalBtn?.addEventListener("click", () => howItWorksModal?.classList.add("hidden"));
    modalOkBtn?.addEventListener("click", () => howItWorksModal?.classList.add("hidden"));

    // Parse URL params on load
    function loadState() {
      const urlParams = new URLSearchParams(window.location.search);
      const dur = urlParams.has("duration") ? parseInt(urlParams.get("duration") || "5", 10) : 5;
      if ([1, 5, 10, 60].includes(dur)) {
        selectedDuration = dur;
        durationBtns.forEach((btn) => {
          const btnSec = parseInt(btn.getAttribute("data-seconds") || "5", 10);
          if (btnSec === dur) {
            btn.classList.add("active");
          } else {
            btn.classList.remove("active");
          }
        });
        if (outputTimeLeft) {
          outputTimeLeft.textContent = selectedDuration.toFixed(1) + "s";
        }
      }
      resetTest();
    }

    window.addEventListener("popstate", loadState);

    // FAQ Accordion Toggle Interaction
    const faqToggleButtons = document.querySelectorAll(".faq-toggle-btn");
    faqToggleButtons.forEach((btn) => {
      btn.addEventListener("click", () => {
        const expanded = btn.getAttribute("aria-expanded") === "true";
        
        // Close other open accordions
        faqToggleButtons.forEach((otherBtn) => {
          if (otherBtn !== btn) {
            otherBtn.setAttribute("aria-expanded", "false");
            const otherContent = otherBtn.nextElementSibling;
            if (otherContent) otherContent.classList.add("hidden");
            otherBtn.querySelector("svg")?.classList.remove("rotate-180");
          }
        });

        // Toggle current accordion
        btn.setAttribute("aria-expanded", !expanded ? "true" : "false");
        const content = btn.nextElementSibling;
        if (content) {
          content.classList.toggle("hidden");
        }
        btn.querySelector("svg")?.classList.toggle("rotate-180");
      });
    });

    // Initial page load
    loadState();
    init();
  </script>

  <!-- Help / How it works Guide Overlay Modal -->
  <div
    id="how-it-works-modal"
    class="fixed inset-0 bg-primary/20 backdrop-blur-sm z-50 flex items-center justify-center p-4 transition-all duration-300 hidden"
  >
    <div class="bg-canvas rounded-2xl max-w-lg w-full p-6 border border-hairline shadow-modal flex flex-col gap-4 animate-scale-in max-h-[85vh] overflow-y-auto z-50">
      <div class="flex justify-between items-center pb-2 border-b border-hairline-soft">
        <h3 class="font-display font-semibold text-lg text-primary font-medium">How the CPS Test Works</h3>
        <button
          type="button"
          id="close-modal-btn"
          class="text-stone hover:text-primary transition-all text-lg cursor-pointer"
        >
          ✕
        </button>
      </div>

      <div class="text-xs text-slate leading-relaxed flex flex-col gap-3">
        <h4 class="font-semibold text-primary">1. Clicks Per Second (CPS) Formula</h4>
        <p>
          Your CPS is calculated by dividing your total number of clicks or taps by the duration of the test: <code>CPS = Total Clicks / Seconds</code>.
        </p>

        <h4 class="font-semibold text-primary">2. Starting the Test</h4>
        <p>
          Choose a duration (1s, 5s, 10s, or 60s) from the header panel, then click or tap anywhere inside the Click Arena. The timer starts automatically on your first click.
        </p>

        <h4 class="font-semibold text-primary">3. Speed Tiers & Classifications</h4>
        <p>
          At the end of the test, your score is evaluated against reflex benchmarks:
        </p>
        <ul class="list-disc pl-4 flex flex-col gap-1">
          <li><strong>Fighter Jet Pilot:</strong> 12+ CPS</li>
          <li><strong>F1 Racer:</strong> 10–12 CPS</li>
          <li><strong>Pro Gamer:</strong> 7–10 CPS</li>
          <li><strong>Casual Clicker:</strong> 4–7 CPS</li>
          <li><strong>Sleepy Sloth:</strong> Under 4 CPS</li>
        </ul>
      </div>
      <button
        type="button"
        id="modal-ok-btn"
        class="button-primary text-xs py-2 px-5 mt-2 self-end cursor-pointer"
      >
        Got it
      </button>
    </div>
  </div>

  <!-- Animation & Effect Styles -->
  <style>
    /* Dark Plum variables override classes */
    .dark-plum-theme {
      --plum-bg: #2d1226;
      --plum-accent: #e040a9;
      --plum-accent-deep: #b82a84;
      --plum-border: rgba(224, 64, 169, 0.25);
      --plum-surface: rgba(30, 9, 23, 0.85);
      --plum-text-muted: #d6cbd3;
      --plum-text-bright: #ffffff;
    }

    #restart-test-btn,
    #share-btn {
      background-color: var(--plum-accent) !important;
      color: #ffffff !important;
    }
    #restart-test-btn:hover,
    #share-btn:hover {
      background-color: var(--plum-accent-deep) !important;
    }

    .duration-btn {
      background-color: transparent;
      border: 1px solid var(--plum-border);
      color: var(--plum-text-muted);
      font-size: 11px;
      font-weight: 600;
      padding: 3px 8px;
      border-radius: 9999px;
      cursor: pointer;
      transition: all 0.2s ease;
    }
    .duration-btn.active,
    .duration-btn:hover {
      background-color: var(--plum-accent);
      color: #ffffff;
      border-color: var(--plum-accent);
      box-shadow: 0 0 8px rgba(224, 64, 169, 0.4);
    }

    /* Radar / Cockpit HUD effect styles */
    .radar-hud-effect {
      background: radial-gradient(
        circle,
        rgba(240, 64, 169, 0.12) 0%,
        rgba(28, 28, 30, 0.97) 100%
      );
      border: 2px solid var(--plum-accent);
    }
    .radar-line {
      position: absolute;
      width: 100%;
      height: 2px;
      background: linear-gradient(
        90deg,
        transparent,
        var(--plum-accent),
        transparent
      );
      top: 0;
      animation: radar-sweep-anim 2.5s infinite linear;
    }
    @keyframes radar-sweep-anim {
      0% {
        top: 0%;
        opacity: 0;
      }
      10% {
        opacity: 1;
      }
      90% {
        opacity: 1;
      }
      100% {
        top: 100%;
        opacity: 0;
      }
    }
    .radar-crosshair {
      position: absolute;
      top: 50%;
      left: 50%;
      width: 40px;
      height: 40px;
      border: 1px solid var(--plum-accent);
      border-radius: 50%;
      transform: translate(-50%, -50%);
      animation: crosshair-pulse 1.2s infinite ease-in-out;
    }
    .radar-crosshair::before,
    .radar-crosshair::after {
      content: "";
      position: absolute;
      background: var(--plum-accent);
    }
    .radar-crosshair::before {
      top: 50%;
      left: -10px;
      width: 60px;
      height: 1px;
    }
    .radar-crosshair::after {
      left: 50%;
      top: -10px;
      width: 1px;
      height: 60px;
    }
    @keyframes crosshair-pulse {
      0%,
      100% {
        transform: translate(-50%, -50%) scale(0.9);
        opacity: 0.7;
      }
      50% {
        transform: translate(-50%, -50%) scale(1.1);
        opacity: 1;
      }
    }

    /* Speedlines (F1 style) */
    .speedline-effect {
      background: #1c1c1e;
    }
    .checkered-flag-bg {
      position: absolute;
      inset: 0;
      opacity: 0.15;
      background-image:
        linear-gradient(45deg, #ffffff 25%, transparent 25%),
        linear-gradient(-45deg, #ffffff 25%, transparent 25%),
        linear-gradient(45deg, transparent 75%, #ffffff 75%),
        linear-gradient(-45deg, transparent 75%, #ffffff 75%);
      background-size: 20px 20px;
      background-position:
        0 0,
        0 10px,
        10px -10px,
        -10px 0px;
    }
    .speedline-effect .line {
      position: absolute;
      background: linear-gradient(
        90deg,
        transparent,
        rgba(224, 64, 169, 0.7),
        transparent
      );
      height: 2px;
      width: 100px;
      animation: f1-line-move 0.4s infinite linear;
    }
    .speedline-effect .line:nth-child(2) {
      top: 25%;
      left: -100px;
      animation-delay: 0s;
    }
    .speedline-effect .line:nth-child(3) {
      top: 45%;
      left: -100px;
      animation-delay: 0.1s;
    }
    .speedline-effect .line:nth-child(4) {
      top: 65%;
      left: -100px;
      animation-delay: 0.2s;
    }
    .speedline-effect .line:nth-child(5) {
      top: 85%;
      left: -100px;
      animation-delay: 0.3s;
    }
    @keyframes f1-line-move {
      0% {
        left: -100px;
        width: 50px;
      }
      100% {
        left: 100%;
        width: 150px;
      }
    }

    /* Pro Gamer neon crosshair effect */
    .neon-effect {
      background: #0d0e12;
      box-shadow: inset 0 0 20px rgba(224, 64, 169, 0.4);
    }
    .neon-ring {
      position: absolute;
      top: 50%;
      left: 50%;
      width: 100px;
      height: 100px;
      border: 2px dashed var(--plum-accent);
      border-radius: 50%;
      transform: translate(-50%, -50%);
      animation: rotating-ring 6s infinite linear;
      opacity: 0.6;
    }
    @keyframes rotating-ring {
      from {
        transform: translate(-50%, -50%) rotate(0deg);
      }
      to {
        transform: translate(-50%, -50%) rotate(360deg);
      }
    }
    .gaming-crosshair {
      position: absolute;
      top: 50%;
      left: 50%;
      width: 24px;
      height: 24px;
      border: 2px solid var(--plum-accent);
      transform: translate(-50%, -50%) rotate(45deg);
      animation: neon-spin-anim 3s infinite linear;
      box-shadow: 0 0 10px var(--plum-accent);
    }
    @keyframes neon-spin-anim {
      0% {
        transform: translate(-50%, -50%) rotate(0deg);
      }
      100% {
        transform: translate(-50%, -50%) rotate(360deg);
      }
    }

    /* Sloth effect */
    .sloth-effect {
      background: #2a2c35;
    }
  </style>
</Layout>
